---
description: castor-kit 项目基础约定，始终生效
alwaysApply: true
---

# castor-kit 项目约定

**完整上下文见 `AGENTS.md`，实现任何新功能前必须阅读。**

castor-kit 是 Node.js/TypeScript + React 的 AI-First 管理后台脚手架：pnpm monorepo，`apps/api`（Fastify 5 + Zod + Drizzle）、`apps/web`（React 19 + Vite + shadcn/ui + Tailwind CSS v4 + motion，见 `docs/frontend-redesign-plan.md`）、`apps/mcp`。所有命令在仓库根目录用 pnpm 执行。

## 关键约定速查

### 后端（apps/api）
- API 路由全部挂在 `/api/admin/...`，routes 文件内禁止自定义 `hasPermission`
- 权限检查统一用 `common/auth.ts`：`import { hasMenuPermission, loginRequired } from '@/common/auth'`，`await hasMenuPermission(request, code)`
- 分层：`db/schema → schema → repository → service → routes`，不得跨层调用（routes 不直接写 SQL）
- 表定义：`db/schema/<domain>/<name>.ts` 的 `pgTable(...)` + `xxxToDict()`，并在 `db/schema/index.ts` 导出
- 时间输出用 `toIso()`（禁止 `Date#toISOString()`），numeric 保持字符串
- 命名：后端目录/文件小写连字符（`component-center`、`scheduled-task`）

### 前端（apps/web）
- UI：shadcn/ui 原子组件（`@/components/ui/*`）+ 业务公共组件（`@/shared/components/*`）+ `lucide-react`；样式只用 Tailwind 语义色类（`bg-card` / `text-muted-foreground` / `bg-brand-soft` …）
- 页面结构照 `apps/web/src/modules/admin/pages/users/index.jsx`：PageHeader → FilterBar → DataTable → FormDialog（react-hook-form + FormFields）→ ImportDialog / ExportDialog
- 新增 shadcn 原子组件：`apps/web/scripts/shadcn-add.sh <组件>`
- API 请求：只用 `@/shared/api/request`（Vite 已配置 `@` alias 指向 `src/`）
- 动态路由：页面必须放在 `modules/<module>/pages/<subdir>/<page>/index.jsx`
- 菜单 `component` 字段格式：`admin/users`、`component_center/admin/list_page`

### RBAC
- 菜单唯一事实源 `apps/api/scripts/seed-rbac.ts`；每次菜单变更后必须运行：`pnpm seed:rbac -- --incremental`
- 超级管理员 `super_admin` 拥有全部权限

### 导入导出（标准列表页必备）
- 后端用 `common/tabular.ts`：`buildTable` / `sendTable` / `readTableFile`（只支持 csv / xlsx）
- 前端用 `@/shared/components/data-transfer/ImportDialog` + `@/shared/components/data-transfer/ExportDialog`
- 文件下载用 `@/shared/utils/file` 中的 `downloadBlobFile`

### 迁移
- 表结构变更：`pnpm db:generate --name <描述>`（不能写 `--`）→ `pnpm db:migrate` → `psql -d castor_kit -c '\d <table>'` 实证

### 禁止行为
- ❌ 询问 PM 路由路径、权限编码、字段类型等技术细节
- ❌ 导入 `@douyinfe/*`（Semi 已下线）或其他 UI 库；使用 `var(--semi-*)`、写死十六进制颜色
- ❌ 前端用 `fetch`/`XMLHttpRequest`（必须用 request.js）
- ❌ 手写迁移 SQL 或迁移不落库
- ❌ 跳过 `pnpm verify` 门禁直接声明完成
